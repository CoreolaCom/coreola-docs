# `getPersistData.ts`

Read a `redux-persist` slice from localStorage.

- `getPersistData(key)` — Reads a `redux-persist` entry and double-decodes its nested JSON. Returns `{}` if the key is missing.

- Source: `src/helpers/getPersistData.ts`
- Import: `import { getPersistData } from 'helpers/getPersistData'`
