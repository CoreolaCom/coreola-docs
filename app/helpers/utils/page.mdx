# `utils.ts`

Small general-purpose utilities.

- `randomIntFromInterval(min, max)` — Inclusive random integer in `[min, max]`.

- Source: `src/helpers/utils.ts`
- Import: `import { randomIntFromInterval } from 'helpers/utils'`
