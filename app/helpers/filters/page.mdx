# `filters.ts`

Helpers for table filter chips.

- `getFilterChipLabel(filter, selectedValues)` — Builds the compact chip text shown for an active table filter — e.g. `Status: Active +2`.

- Source: `src/helpers/filters.ts`
- Import: `import { getFilterChipLabel } from 'helpers/filters'`
