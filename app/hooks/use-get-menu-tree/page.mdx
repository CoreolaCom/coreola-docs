# `useGetMenuTree`

Builds the dashboard menu tree — grouped, translated, RBAC-filtered, with icons and custom item behaviors merged in.

```ts
useGetMenuTree(): MenuTree
```

**Returns** — A `MenuTree`.

**Use when** — When rendering the main sidebar or any dashboard navigation surface.

- Source: `src/hooks/useGetMenuTree.ts`
- Import: `import useGetMenuTree from 'hooks/useGetMenuTree'`
